name: care_network_demo
description: Free demo nursing app
version: 1.0.0+1

environment:
  sdk: ">=2.20.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0      # Google Sheets / API calls
  fluttertoast: ^8.2.0
  provider: ^6.0.0   # State management
